[phases.setup]
nixPkgs = ["python311"]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["python -m pip install --no-cache-dir -r requirements.railway.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
