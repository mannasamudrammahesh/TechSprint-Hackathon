[phases.setup]
nixPkgs = ["python311", "pip", "ffmpeg"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.railway.txt"]

[phases.build]
cmds = ["echo 'Build complete - Railway optimized'"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
